---
description: Watch CI check runs for a git ref
argument-hint: "<ref> [poll_interval]"
allowed-tools: mcp__plugin_gh-channels_gh-channels__watch_ci
---

Call the watch_ci tool. Use the repo info from the gh-channel server instructions.
Ref: $1. Poll interval: $2 (if provided, otherwise omit).
